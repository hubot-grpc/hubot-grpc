procedure_options:
  - procedure: .pkg.test.second
    defaults:
      first: { "text": "first" }
  - procedure: .pkg.test.fourth
    custom_calls:
      - alias: "fourth"
        defaults:
          integers: [2,3,4]
      - alias: "default"
    defaults:
      integers:
        - 1
        - 2
        - 3
